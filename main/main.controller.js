var app = angular.module('SiteApp');

app.controller('MainController', MainController );

MainController.$inject = ['$scope', '$rootScope', '$route', '$routeParams', '$http' ];

function MainController($scope, $rootScope, $route, $routeParams, $http ) {
	
	var mv = this;



};
